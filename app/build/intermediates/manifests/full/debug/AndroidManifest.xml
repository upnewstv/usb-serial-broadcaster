<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="net.signagewidgets.serial"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="15"
        android:targetSdkVersion="22" />

    <uses-feature android:name="android.hardware.usb.host" />

    <application
        android:icon="@drawable/icon"
        android:label="Signage Widgets Serial Device Support"
        android:theme="@style/Theme.Transparent" >
        <activity
            android:name="net.signagewidgets.serial.activities.AttachActivity"
            android:excludeFromRecents="true"
            android:noHistory="true"
            android:stateNotNeeded="true" >
            <intent-filter>
                <action android:name="android.hardware.usb.action.USB_DEVICE_ATTACHED" />
            </intent-filter>

            <meta-data
                android:name="android.hardware.usb.action.USB_DEVICE_ATTACHED"
                android:resource="@xml/device_filter" />
        </activity>

        <service
            android:name="net.signagewidgets.serial.services.SerialService"
            android:exported="true" >
        </service>

        <receiver android:name="net.signagewidgets.serial.broadcast.DettachReceiver" >
            <intent-filter>
                <action android:name="android.hardware.usb.action.USB_DEVICE_DETACHED" />
            </intent-filter>
        </receiver>
    </application>

</manifest>